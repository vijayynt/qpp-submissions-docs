import submissionsApiRoutes from './submissions-api-routes';
import webIntefaceApiRoutes from './web-interface-api-routes';

let allRoutes = {
  'Submissions API': submissionsApiRoutes,
  'Web Interface API': webIntefaceApiRoutes
};

export default allRoutes;
